'use client';

/**
 * TemplateScripts is a placeholder for legacy script loader.
 * All dynamic behaviors (sticky header, scroll-to-top, dropdown toggle, carousel)
 * have been migrated to native React implementations to eliminate render-blocking JS.
 */
export default function TemplateScripts() {
  return null;
}
